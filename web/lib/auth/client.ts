/**
 * Better Auth Client - Convex backend
 * Re-exports from auth-client for compatibility
 */

export {
  authClient,
  signIn,
  signUp,
  signOut,
  useSession,
  $fetch,
} from "../auth-client";
