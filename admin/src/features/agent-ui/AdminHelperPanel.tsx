"use client";

export function AdminHelperPanel({
  isBusy,
  pendingCount,
  onRunPrompt,
  onRunTask,
}: {
  isBusy: boolean;
  pendingCount: number;
  onRunPrompt: (prompt: string) => void;
  onRunTask: (task: string) => void;
}) {
  return null;
}
